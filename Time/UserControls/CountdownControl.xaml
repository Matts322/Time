<UserControl x:Class="Time.UserControls.CountdownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox x:Name="CountdownTime" HorizontalAlignment="Center" FontSize="70" MinWidth="45" Foreground="White" Margin="0 10 0 0" Cursor="Arrow"/>
            <ToggleButton Content="Start/Stop" Checked="ToggleButton_Checked" Unchecked="ToggleButton_Unchecked" Height="40" Margin="20" HorizontalAlignment="Center" Style="{StaticResource MaterialDesignFlatDarkBgButton}"/>
            <Button x:Name="ResetButton" Content="Reset" HorizontalAlignment="Center" MinHeight="40" MinWidth="100" Margin="0,10,0,0" Click="Button_Click" Style="{StaticResource MaterialDesignFlatDarkBgButton}"/>
            <TextBlock Padding="15"/>
        </StackPanel>

    </Grid>
</UserControl>
